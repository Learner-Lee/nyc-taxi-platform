# Spark 默认配置
# 提交 Job 时这些参数自动生效，无需每次手动指定

# History Server — 保存已完成 Job 的执行计划，方便事后分析
spark.eventLog.enabled                  true
spark.eventLog.dir                      hdfs://namenode:9000/spark-logs
spark.history.fs.logDirectory           hdfs://namestore:9000/spark-logs

# Hive 集成 — 让 Spark SQL 能直接查 Hive 表
spark.sql.warehouse.dir                 hdfs://namenode:9000/user/hive/warehouse
spark.hadoop.hive.metastore.uris        thrift://hive-metastore:9083

# 性能优化（默认开启，后续实验会手动关闭对比效果）
spark.sql.adaptive.enabled              true
spark.sql.adaptive.coalescePartitions.enabled  true
spark.sql.adaptive.skewJoin.enabled     true

# Parquet 优化
spark.sql.parquet.filterPushdown        true
spark.sql.parquet.mergeSchema           false

# Shuffle 分区数（小数据集用小值，后续实验会调整）
spark.sql.shuffle.partitions            200

# 序列化（Kryo 比 Java 序列化快 10x）
spark.serializer                        org.apache.spark.serializer.KryoSerializer

# 动态资源分配（学习环境关闭，固定资源更好预测）
spark.dynamicAllocation.enabled         false
